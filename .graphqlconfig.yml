projects:
  pmw:
    schemaPath: src/graphql/schema.json
    includes:
      - graphql
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: angular
        generatedFileName: src/app/API.service.ts
        docsFilePath: .
